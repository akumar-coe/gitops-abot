apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Ensure the operator runs in this namespace
namespace: abot-system
# List every manifest you want ArgoCD to apply from this folder
resources:
 - namespace.yaml
 - rbac.yaml
 - deployment.yaml
 # - service.yaml   # uncomment if you actually use a Service
